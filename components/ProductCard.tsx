'use client';import Image from 'next/image';import Link from 'next/link';import type {Product} from '@/lib/types';import {money} from '@/lib/format';
export default function ProductCard({p}:{p:Product}){function move(e:React.MouseEvent<HTMLAnchorElement>){const el=e.currentTarget;const r=el.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${-y*4}deg) rotateY(${x*5}deg) translateY(-6px)`}function leave(e:React.MouseEvent<HTMLAnchorElement>){e.currentTarget.style.transform='perspective(900px) rotateX(0deg) rotateY(0deg) translateY(0px)'}return <Link href={`/product/${p.slug}`} onMouseMove={move} onMouseLeave={leave} className="watch-card interactive group block transition-transform duration-500"><div className="shine relative aspect-square overflow-hidden border border-white/10 bg-[#050505]"><Image src={p.image} alt={`${p.brand} ${p.name}`} fill className="object-cover opacity-70 saturate-0 transition duration-700 group-hover:scale-105 group-hover:opacity-95"/><div className="absolute inset-0 bg-radial from-transparent to-black/70"/></div><div className="mt-5 text-center"><p className="text-[9px] font-semibold uppercase tracking-[.24em] text-[var(--gold)]">{p.brand}</p><h3 className="serif mt-2 text-2xl leading-none text-white/90">{p.name}</h3><p className="mt-2 text-[10px] uppercase tracking-[.16em] text-white/38">Ref. {p.ref}</p><p className="serif mt-2 text-lg text-white">{money(p.price)}</p></div></Link>}
