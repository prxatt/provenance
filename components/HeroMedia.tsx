'use client';import Image from 'next/image';import {motion,useScroll,useTransform} from 'framer-motion';
export default function HeroMedia({src,alt}:{src:string;alt:string}){const {scrollYProgress}=useScroll();const y=useTransform(scrollYProgress,[0,.7],[0,-120]);const scale=useTransform(scrollYProgress,[0,.7],[1,1.08]);return <motion.div style={{y,scale}} className="absolute inset-0"><Image src={src} alt={alt} fill priority className="object-cover opacity-90 saturate-75"/><div className="absolute inset-0 bg-gradient-to-r from-black via-black/45 to-black/20"/><div className="absolute inset-0 bg-[radial-gradient(ellipse_at_60%_35%,rgba(201,169,110,.08),transparent_45%)]"/></motion.div>}
