'use client';import {useState} from 'react';import type {Product} from '@/lib/types';import {money} from '@/lib/format';
export default function CheckoutForm({product}:{product:Product}){const[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const payload={...Object.fromEntries(f),productSlug:product.slug,amount:product.price};const res=await fetch('/api/orders',{method:'POST',body:JSON.stringify(payload),headers:{'content-type':'application/json'}});setLoading(false);if(res.ok)setDone(true)}if(done)return <div className="panel mt-10 p-8"><p className="kicker">Request Secured</p><h2 className="serif mt-3 text-4xl">Concierge review started.</h2><p className="body mt-4">This prototype logged your order request locally. Production should connect this to Stripe Checkout, escrow, and insured fulfillment.</p></div>;return <form onSubmit={submit} className="mt-10 space-y-4"><input className="field" name="email" type="email" required placeholder="Email"/><input className="field" name="name" required placeholder="Full name"/><input className="field" name="phone" placeholder="Phone"/><input className="field" name="shippingCity" placeholder="Shipping city / country"/><textarea className="field min-h-28" name="note" placeholder="Questions, escrow preference, delivery timing"/><button className="btn w-full"><span>{loading?'Securing…':`Request Purchase ${money(product.price)}`}</span><span>→</span></button><div className="panel p-5"><p className="text-sm text-white/85">All sales final. Fully insured shipping.</p><p className="body mt-2 text-sm">Payment is not captured in this prototype. The production flow should use Stripe Checkout or escrow for high-value transactions.</p></div></form>}
