'use client';import {useState} from 'react';
export default function AccessForm(){const[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const res=await fetch('/api/access',{method:'POST',body:JSON.stringify(Object.fromEntries(f)),headers:{'content-type':'application/json'}});setLoading(false);if(res.ok)setDone(true)}if(done)return <div className="panel mt-10 p-6"><p className="kicker">Request Received</p><p className="body mt-3">Your collector access request is logged for review.</p></div>;return <form onSubmit={submit} className="mt-10 max-w-lg space-y-3"><input className="field" name="email" type="email" required placeholder="Email address"/><input className="field" name="name" placeholder="Name"/><textarea className="field min-h-28" name="interest" placeholder="What are you looking for?"/><button className="btn w-full"><span>{loading?'Requesting…':'Request Access'}</span><span>→</span></button></form>}
