import ProductCard from '@/components/ProductCard';import Revealer from '@/components/Revealer';import {getProducts} from '@/lib/data';
export default async function Collection(){const products=await getProducts();return <main className="pt-32"><section className="container-lux"><Revealer className="flex flex-wrap items-end justify-between gap-8"><div><h1 className="h1">Collection</h1><p className="body mt-4 max-w-md">Curated timepieces and fine jewelry. Each with a story. Each verified.</p></div><div className="flex gap-7 text-[10px] font-semibold uppercase tracking-[.24em] text-white/45"><span className="gold">All</span><span>Watches</span><span>Jewelry</span></div></Revealer><div className="mt-16 grid gap-x-10 gap-y-16 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div></section></main>}
